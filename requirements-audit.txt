# Dependency-audit scope for the weekly pip-audit gate (.github/workflows/security.yml).
#
# This is the analysis stack that CI (tests/run_tests.py) actually exercises.
# torch/torchvision from requirements.txt are deliberately excluded here:
# resolving them pulls the multi-GB CUDA wheel set into the audit environment
# on every run. They are exercised on Colab, where the runtime image is
# maintained by Google; revisit if the repo ever ships torch pinned builds.

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
scikit-image>=0.21
matplotlib>=3.7
pyyaml>=6.0
